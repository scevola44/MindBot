# MindBot

A self-hosted .NET Worker Service that turns Telegram messages into notes in
an Obsidian vault backed by a Git repository. Runs via docker compose on a
private Linux server. Targets the current .NET LTS.
